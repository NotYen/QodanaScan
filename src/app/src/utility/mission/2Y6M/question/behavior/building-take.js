/* import `Audio` source */
import audio from '../../../../../assecs/source/take-2.m4a';

const config = {
    illustrate: {
        text: "伸出手掌(手掌朝上)，示意孩子積木放大人手上",
        audio: audio
    },
    reaction: []
}

export default config;
